Rewrite the user's query for {target} as a concise, specific, and retrieval-optimized search query.

Rules:
1. Preserve the original intent and meaning.
2. Resolve any pronouns (it, its, that, this, uska, woh, etc.) using the conversation history provided in the query.
3. If the query is not in English, translate it to English first, then rewrite.
4. Return ONLY the rewritten query. No explanation, no preamble.

Query + History:
{query}

Rewritten query: